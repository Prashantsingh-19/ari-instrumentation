name = "ari-instrumentation"
main = "src/worker.js"
compatibility_date = "2025-04-01"

# Shared KV namespace — must match the portfolio's SESSIONS binding
[[kv_namespaces]]
binding = "ANALYTICS"
id = ""  # same namespace ID as portfolio's SESSIONS
